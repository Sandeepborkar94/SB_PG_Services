spring:
  datasource:
    url: jdbc:h2:mem:dcbapp
    username: sa
#    password: springpass
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    

application:
  name: customer-portal

server:
  port: 8080
